# Configurazione Database MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=test
DB_USER=user
DB_PASSWORD=password

# Configurazione Server
PORT=4000

# Note:
# - Copia questo file come .env e modifica i valori secondo la tua configurazione
# - Non committare mai il file .env nel repository per motivi di sicurezza
# - Per Docker, assicurati che DB_HOST punti al nome del servizio (es. 'mysql' nel docker-compose)